name: Deploy Dashboard to GitHub Pages

on:
  push:
    branches: [main]
  workflow_run:
    workflows: ["FX Discovery"]   # Runs after discovery pipeline completes
    types: [completed]

permissions:
  contents: read
  pages:    write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install workspace deps
        run: npm ci

      - name: Build dashboard (apps/web)
        run: npm run build -w @fx/web

      - name: Copy live data snapshot into the build
        run: |
          mkdir -p apps/web/dist/data
          cp data/events.json apps/web/dist/data/events.json
          cp data/leads.json  apps/web/dist/data/leads.json

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/web/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
